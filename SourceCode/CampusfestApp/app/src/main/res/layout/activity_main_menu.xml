<?xml version="1.0" encoding="utf-8"?>
<ScrollView  xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background_color"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/background_color"
        android:fitsSystemWindows="true"
        android:orientation="vertical"
        tools:context=".MainMenu">

        <include layout="@layout/template_logo" />

        <Button
            style="@style/Button"
            android:id="@+id/lineUp"
            android:text="@string/title_activity_line_up"
            android:onClick="openLineUp"/>
        <Button
            style="@style/Button"
            android:id="@+id/timetable"
            android:text="@string/title_activity_timetable"
            android:onClick="openTimetable"/>
        <Button
            style="@style/Button"
            android:id="@+id/pay"
            android:text="@string/title_activity_pay" />
    </LinearLayout>
</ScrollView>